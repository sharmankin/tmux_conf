selectp -t 0
kill-pane -a
splitw -h sudo lnav
splitw -v sudo -i ; send 'clear && tput cup $LINES' Enter ; select-pane -t +1
splitw -v sudo htop ; select-pane -t 0 ; send 'clear && tput cup $LINES' Enter
display-panes
