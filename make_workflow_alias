#!/usr/bin/env bash

mkdir -p ~/.config/run

if [[ -d ~/.bashrc.d ]]; then
  tee ~/.bashrc.d/50-tmux-workflow-alias <<EOF
alias wf='tmux -S ~/.config/run/tmux attach -d -t $USER-workflow 2>/dev/null || tmux -S ~/.config/run/tmux new -t $USER-workflow'
EOF
fi
